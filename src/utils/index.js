import * as format from './format';
import * as image from './image';import * as log from './log';
import * as timer from './timer';
import * as localStorage from './localStorage';

export default {
	format,
	image,
	log,
	timer,
	localStorage
};